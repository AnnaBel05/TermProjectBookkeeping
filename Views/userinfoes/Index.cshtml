@model IEnumerable<TermProjectBookkeeping.userinfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <div> Фамилия </div>
        </th>
        <th>
            <div> Имя </div>
        </th>
        <th>
            <div> Отчество </div>
        </th>
        <th>
            <div> E-mail </div>
        </th>
        <th>
            <div> Пароль </div>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.userrole.rolename)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lastname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.patronymic)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.userrole.rolename)
        </td>
        <td>
            @if (User.Identity.IsAuthenticated)
            {
                if (User.IsInRole("Суперюзер"))
                {
                    <p>
                        <button>@Html.ActionLink("Редактировать", "Edit", new { id = item.id })</button>
                        <button> @Html.ActionLink("Детали", "Details", new { id = item.id })</button>
                        <button> @Html.ActionLink("Удалить", "Delete", new { id = item.id })</button>
                    </p>
                }
            }
        </td>
    </tr>
}

</table>
